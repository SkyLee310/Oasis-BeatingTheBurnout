Tag from .. Use via `window.OasisDS.Tag` (bundle loaded from the root `_ds_bundle.js`).

A small stroked pill for metadata — dates, counts, short labels.

## Props

```ts
interface TagProps {
  children: React.ReactNode;
  /** Fill colour. `neutral` is the plain surface pill. */
  tone?: "green" | "amber" | "red" | "neutral" | "yellow";
}
```
