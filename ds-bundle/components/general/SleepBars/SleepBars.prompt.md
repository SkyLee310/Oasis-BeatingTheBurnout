SleepBars from .. Use via `window.OasisDS.SleepBars` (bundle loaded from the root `_ds_bundle.js`).

A week of sleep, each night in its own stroked track.

## Props

```ts
interface SleepBarsProps {
  data: SleepDatum[];
  /** Overall height in px. */
  height?: number;
}
```
