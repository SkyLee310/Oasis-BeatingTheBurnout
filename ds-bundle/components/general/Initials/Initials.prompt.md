Initials from .. Use via `window.OasisDS.Initials` (bundle loaded from the root `_ds_bundle.js`).

The avatar stand-in: a yellow disc with the user's initials.

## Props

```ts
interface InitialsProps {
  /** Diameter in px. Type scales with it. */
  size?: number;
  /** One or two letters. */
  initials?: string;
}
```
