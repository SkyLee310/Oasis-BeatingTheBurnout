node_modules/
dist/
dist-ds/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
ds-bundle/
.ds-sync/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Agent workspace state. launch.json is the shared dev-server config and is
# checked in so anyone can open the preview; everything else here is scratch.
/.claude/*
!/.claude/launch.json
