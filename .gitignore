node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
.worktrees/

# Standalone clickable prototype for PM review — self-deployed separately,
# never tracked here.
/prototype/

# Design system sync tooling & transient artifacts
/.ds-sync/
ds-bundle/_ds_needs_recompile
ds-bundle/.ds-bundle
